# AgentFleet — Production-Readiness Plan (all builds → deploy last)

User decision (2026-07-12): build **every** optional item to a fully production-ready, feature-complete web app, THEN deploy (AWS → GCP) as the very last step. Sequenced by dependency + testing friction (auth/rate-limit deliberately late so feature executors test against the open local API; auth wraps everything at the end).

Working model: Opus/Fable = brain (specs + review + live verification), Sonnet subagents = executors. Each chunk: spec → delegate → independently verify (pytest + build + live smoke) → commit → push → confirm CI green.

## Phase 9 — Reliability & web access (self-contained, high value)
- **A. Robust web access** ← FIRST. Pluggable search provider (env `WEB_SEARCH_PROVIDER=tavily|exa|searxng`, default searxng so it works with no key): add Tavily (best for agents) + Exa support; SearXNG stays as fallback. New `fetch_url` tool via **trafilatura** (pure-Python clean extraction, no headless browser — container-friendly; Crawl4AI noted for JS-heavy pages later). Give deep-research the fetch tool. Fixes the citation pain. USER ACTION: optional Tavily/Exa API key for best results.
- **E. Postgres LangGraph checkpointer** — replace MemorySaver with a Postgres saver so agent graph state is durable + cross-replica.
- **D. Durable orchestration** — wire arq + Redis worker so orchestrator runs survive API restarts (ADR-004); keep in-process as fallback.
- **G. Seed eval cases + real CI eval gate** — a few golden cases per built-in agent so `scripts.run_evals` is a real regression gate, not a no-op.
- **F1. Ops hygiene** — structured JSON logging + request-ID middleware; env-configurable CORS (`CORS_ORIGINS`).

## Phase 10 — Feature builds
- **H. Extra roster agents** — SQL Analytics (NL→SQL over a sandboxed read-only DB), Competitor Monitor (scheduled scrape+diff+digest — pairs with I), Meeting-Notes→CRM (transcript→structured), Outreach (research→draft→HITL). Each with an eval case.
- **I. Webhooks + scheduled runs** — inbound webhook trigger per workflow + cron-scheduled runs (turns chat app into an automation platform). Depends on D (worker) for scheduling.
- **J. Artifacts side panel** — sandboxed rendering of agent-generated markdown/code/charts (Vega-Lite) in a resizable panel.
- **L. Prompt playground + A/B** — side-by-side prompt/model compare + versioned experiments.
- **M. Second-SDK agent** — one agent on Pydantic AI (or Claude Agent SDK) to show framework breadth beyond LangGraph.
- **N. Voice agent** — Vapi/Retell-wrapped voice interface (rising German-JD theme). Scoped demo.
- **K. ⌘K command palette + polish** — cmdk palette, designed empty states, changelog page, keyboard nav.

## Phase 11 — Quality
- **O. E2E tests** — Playwright covering core flows (login → chat with tool → upload doc → mission with approval → build+red-team agent).
- **P. Error monitoring** — Sentry (backend + frontend) with request-ID correlation.
- **Pagination (nice-11)** — limit/offset on list endpoints (agents, runs, documents, conversations, versions) with sane defaults.
- **Load testing (nice-15)** — a k6/locust script exercising chat + list endpoints + a short results note in docs.

## Phase 12 — Production hardening wrapper (right before deploy)
- **B. API auth + resource ownership** — verify the Auth.js session (HS256 JWT shared secret) on every `/api/v1/*` route; set + enforce `user_id` on conversations/documents/agents/runs; gate app pages behind login. One migration to backfill ownership. THIS is the big one; do it once, comprehensively, at the end.
- **C. Rate limiting** — per-user + per-IP limits (slowapi/Redis) on chat + public invoke.
- **F2. Deploy-safety** — Alembic migration Job / init-container (multi-replica safe), readiness probe with DB check, fastembed pre-warm, **SSR/browser URL split (nice-16)** (`INTERNAL_API_URL` for web-container SSR vs `NEXT_PUBLIC_API_URL` for the browser).

## Phase 13 — Q. Cloud deploy (LAST)
- AWS via Terraform at interview time (~$100 credit); GCP configs after (~3mo free). ADR-007.
- Managed Postgres (RDS / Cloud SQL) provides **backups + HA (nice-14)** out of the box; document the restore path.

## .env keys for the new features
All added to `.env.example` (commented/optional, grouped): AUTH_SECRET (shared with web, API-side), SLACK_WEBHOOK_URL, HUBSPOT_ACCESS_TOKEN, GITHUB_TOKEN, ANALYTICS_DATABASE_URL, OPENAI_API_KEY (optional Whisper), VAPI_API_KEY, WEBHOOK_SIGNING_SECRET, SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN, INTERNAL_API_URL. Every one degrades gracefully when blank.

## Status log
- (start 2026-07-12) Phase 9 kicking off with A.
- ✅ A done (robust web access: pluggable Tavily/Exa/SearXNG + fetch_url via trafilatura, SSRF-guarded; deep-research live-verified search→fetch→cite). Chose over Agent Reach (ToS-violating cookie-scraping CLI — wrong for deployed multi-tenant).
- ✅ G done (seed_evals.py idempotent golden cases; CI eval gate now seeds-then-runs).
- ✅ F1 done (JSON logging + X-Request-ID middleware + env CORS_ORIGINS). 43 tests.
- ✅ E done (durable Postgres LangGraph checkpointer). 47 tests.
- ✅ D done (durable arq/Redis orchestration: app/worker.py + services/queue.py; ORCHESTRATOR_MODE=arq|inprocess, default inprocess for dev; compose/k8s worker service set arq; LIVE durability PROVEN — run completed across an API kill+restart). **53 tests, Phase 9 COMPLETE.**
- ✅ Phase 10 H DONE (all roster agents): H1 SQL Analytics (sql_query read-only tool + demo schema), H2 Competitor Monitor, H3 Meeting-Notes→CRM (push_to_crm), H4 Outreach — plus send_slack/push_to_crm tools that degrade gracefully w/o keys. **8 built-in agents, 62 tests.** Live-verified.
- ✅ Phase 10 I (scheduled runs half) DONE: scheduled_runs table + /api/v1/schedules CRUD + services/scheduler.py (is_due + poll_due_schedules) + arq cron job (every minute) + /automations UI. 70 tests. Live-verified poller creates a run from a due schedule. (Autonomous agents on a cron — the "automation platform" story.)
- ✅ Phase 10 I FULLY DONE (schedules + webhooks): inbound webhook triggers (Webhook model hash-only secret, /api/v1/webhooks CRUD + /api/v1/hooks/{id} bearer-auth trigger with {payload} substitution, /automations UI). Auth is bearer-secret (hash-only storage, like API keys), not HMAC — documented. 72 tests.
- ✅ Migration hygiene fix: migrations/env.py include_object now skips externally-managed tables (checkpoint*, analytics_*) so autogenerate stops proposing to drop them (`alembic check` = clean). No more manual migration trimming.
- ✅ Phase 10 J DONE (sandboxed artifacts side panel): `apps/web/lib/artifacts.ts` (streaming-safe fence parser + escape-then-transform safe markdown renderer w/ URL allowlist) + `components/artifact-panel.tsx` (code `<pre>`, safe markdown, lazily-imported vega-embed charts kept out of the main bundle, bad-spec → source fallback; drag-resize + mobile overlay) + surgical `chat-ui.tsx` (assistant content → text/artifact chips; streaming loop untouched). Added deps vega/vega-lite/vega-embed (dynamic-import only). LIVE-verified in browser: python/markdown/vega-lite chips all render (real bar chart A=3/B=5/C=2), panel open/close reflow, graceful provider-error path; web build green, console clean. Commit 1afd471.
- ✅ Phase 10 K DONE (palette + polish): `components/command-palette.tsx` (global ⌘K/Ctrl+K, hand-rolled no-dep, type-filter + arrow/Enter/Esc + full ARIA + focus restore, mounted in root layout, renders null when closed) + `components/empty-state.tsx` (shared designed block, applied to Chat/Documents/Missions/Agents/Evals/Automations/Templates; Usage left alone — inline not block-level) + `app/changelog/page.tsx` (static honest phase-by-phase timeline, reachable via palette). LIVE-verified: palette open/filter/navigate, Missions empty state, changelog render; web build green (/changelog static route), console clean. Commit 637752c.
- ✅ Phase 10 L DONE (prompt playground + A/B): `routes/playground.py` (stateless POST /run reusing chat's provider+cost machinery, 502-with-detail per variant; GET /models best-effort provider list w/ config fallback, never raises; experiments CRUD) + `playground_experiments` migration (fcf2c79da7bc) + `/playground` page (side-by-side variants, shared prompts, load-from-agent seeding, parallel run-both, usage footers, saved-experiments save/reload) + palette entry ⚗️. REVIEW CATCH: worker's happy-path test called the live proxy (localhost:3001) — would fail CI (no proxy there); fixed to mocked provider. LIVE-verified: two-model A/B run, load-from-agent (Creative Writer seeded model+prompt), save + reload round-trip. 76 tests, alembic check clean, web build green. Commit 2aa4c86.
- ✅ Phase 10 M DONE (2nd SDK runtime): `services/pydantic_runtime.py` on pydantic-ai-slim 2.11 (OpenAIChatModel+OpenAIProvider, real delta streaming, web_search+search_documents wrapped from app.tools, budget check, history replay, partial-turn persistence on disconnect; NOTE: no tool_call/tool_result SSE frames — intentional narrow scope, tool cards won't show for this runtime). `Agent.runtime` column (default langgraph, migration 60038d765942) + single dispatch point in chat.py. New builtin `fact-checker` agent (9 total) + eval case. Tests hermetic (79 passed). LIVE-verified in UI: false claim → FALSE verdict with cited Wikipedia source (wrapped web_search ran inside Pydantic AI), usage footer intact, zero frontend changes. Commit 021c4ff.
- ✅ Phase 10 N DONE (voice, keys-optional): `routes/voice.py` GET /config gates on VAPI_PUBLIC_KEY (blank → {enabled:false}; set → public key + Vapi transient-assistant config; api key never exposed) + `/voice` page (designed not-configured state; Start/End call via dynamic @vapi-ai/web when key present, call-start/call-end/error wired) + palette 🎙 + VAPI_PUBLIC_KEY env rows. 82 hermetic tests, /voice static route, live-verified disabled state. Real call untestable until user adds a Vapi key (expected). Commit 07dcaac. **PHASE 10 COMPLETE.**
- ✅ Phase 11 O DONE (Playwright E2E): 20 tests, two projects — deterministic (17: smoke all pages, palette, doc upload→ingest, schedules+webhooks create/delete) + llm (3: streamed chat w/ usage line, artifact chip→panel, playground A/B), `E2E_SKIP_LLM=1` drops llm project (`npm run e2e:fast`). No webServer block (stack assumed up; e2e/README.md). Zero app-code changes; role/text selectors; Next-16 dev portal handled via shadow-DOM error selectors. NOT in CI (no web/proxy stack there — future). Both suites verified twice (worker + orchestrator). NOTE for Phase 12 B: E2E will need a test-user/auth-bypass strategy once pages are gated; /chat and /missions lack an h1 (candidate polish). Commit cdcc366.
- ✅ Phase 11 pagination DONE: uniform limit/offset (ge=1 le=200 / ge=0) on 10 DB-backed list endpoints, defaults preserve prior behavior, deterministic ORDER BY everywhere (fixed keys list's missing order_by), X-Total-Count header (no envelope change), CORS exposes X-Total-Count+X-Request-ID. Skips documented (tools/templates/models/voice/usage aggregates/budgets mirror). 85 tests + 17/17 E2E, frontend untouched. Commit 6d29755.
- ✅ Phase 11 P DONE (Sentry, DSN-gated no-op): backend `observability.py` (init after setup_logging, request-ID tag via before_send, pii off, 0.1 traces) + frontend Next-16 `instrumentation{,-client}.ts` convention w/ DSN-gated DYNAMIC import (bundle-clean when disabled); sourcemap upload deferred to deploy. 90 tests, E2E 17/17, no-op verified (log line + clean console). Real delivery unverified until user adds a DSN. Commit 5f49d5c.
- ✅ Phase 11 load test DONE: `load/k6-api.js` (k6 v2.1.0 via brew; lists ramp-to-20-VUs p95<300ms, writes 5-VU conversation-create p95<500ms, chat_smoke opt-in CHAT=1 so default never hits the LLM) + `docs/LOAD_TEST.md` with real numbers: lists p95 94ms @ ~414 rps, writes p95 96ms, 0% errors, 48.6k reqs, 100% checks — 3-5x threshold headroom on a single uvicorn worker. Commit f9414d5. **PHASE 11 COMPLETE.**
- ✅ Phase 12 B COMPLETE (auth + ownership, B1 backend 9d1549e + B2 frontend 0ef29ca): Bearer HS256 JWT (PyJWT, alg-pinned, fail-closed 503) via shared AUTH_SECRET; email→User create-on-first-call; centralized `app/ownership.py` visibility (owner|null|builtin-readable, builtins never mutable, invisible→404 no-leak) across all 15 routers; user_id on agents/documents/runs (migration 2fc38a3e8b1b) + Conversation cascade fix; web mints 15-min tokens (app/api/token + lib/api.ts apiFetch/apiFetchServer, single contract def); pages gated via proxy.ts (Next's renamed middleware) + authorized callback — /, /changelog, /health public; E2E forges a REAL Auth.js session cookie in global setup (encode(), salt=cookie name) — zero bypass code in app; auth.spec.ts covers signed-out redirect + tokenless 401. 98 API tests + 21/21 E2E. KNOWN LIMITATIONS (want admin role later): builtin sub-resource writes (keys/evals/budgets) open to any signed-in user; /usage is global aggregate.
- ✅ Phase 12 C DONE (rate limiting, 6c9c754): slowapi keyed per-JWT-email else IP (XFF untrusted by default), Redis storage w/ memory fallback, RATE_LIMIT_CHAT/UPLOAD/PUBLIC env + DISABLED switch, hand-built Retry-After (slowapi headers_enabled=True 500s dict routes — documented); k6 auth regression fixed (API_TOKEN). 102 tests, live 429 verified.
- ✅ Phase 12 F2 DONE (deploy-safety, 2714172): one-shot migrate (compose service + k8s Job, entrypoint de-raced; smoke proved exit-0-before-api), /health/ready (SELECT 1, 2s timeout; /health stays DB-free), fastembed pre-warm (background, EMBEDDINGS_PREWARM guard), INTERNAL_API_URL SSR split. 106 tests, E2E 18/18. ENV NOTE: this Mac's Docker Desktop VM SIGILLs cryptography's Rust ext in ANY container (repro'd on bare python:3.12-slim) — containerized api unserveable locally until Docker Desktop updated; CI/cloud unaffected. **PHASE 12 COMPLETE — ALL PLANNED BUILDS (9→12) SHIPPED.**
- **REMAINING: Phase 13 ONLY (cloud deploy, on user's go)** + user demo feedback fixes. USER TODOs: rotate Google OAuth client secret (from the 2026-07-12 leak); optional keys (TAVILY, VAPI_PUBLIC_KEY, SLACK_WEBHOOK_URL, HUBSPOT, SENTRY_DSNs); update Docker Desktop (SIGILL note above). Polish backlog: stale changelog page (stops at 10 K), /chat + /missions h1, real GET /agents/{id}, admin role (builtin sub-resource writes + global /usage).
- KNOWN MINOR: competitor-monitor can burn MAX_TOOL_ROUNDS (5, shared const in chat.py/graph_runtime) on research before calling send_slack on the free Groq model — revisit as a per-agent round budget or tighter prompt when I (scheduled runs) makes it autonomous.
- KNOWN MINOR (fix in Phase 12 B when resource-deletion/ownership lands): Conversation→Message ORM relationship lacks cascade/passive_deletes, so ORM `session.delete(conversation)` tries to NULL the NOT NULL messages.conversation_id instead of using the DB's ON DELETE CASCADE — add `cascade="all, delete-orphan"` + `passive_deletes=True` (there's no delete-conversation endpoint yet, so it doesn't bite today).
- SECURITY: AUTH_SECRET rotated 2026-07-12 (executor leaked it via `docker compose config`). USER TODO: rotate the Google OAuth client secret in console + paste into .env. Executor-instruction reminder: NEVER run `docker compose config` without redirecting to /dev/null (it interpolates .env secrets).
- User needs (optional, for best web search): a Tavily or Exa API key in .env (TAVILY_API_KEY / set WEB_SEARCH_PROVIDER=tavily). Works on SearXNG fallback without one.
- ✅ UI-6 COMPLETE (user demo feedback round 1): per-agent icons — new `components/agent-visual.tsx` (AGENT_VISUALS slug→icon+hue map, AgentGlyph xs/sm/md, letter-chip fallback only for user-created agents; single hueForSlug source) + 14 new Lucide-path glyphs in icons.tsx, applied to landing roster (with hover scale/-rotate flourish), chat agent strip, agents grid, missions task chips. Hero rebuilt: hero-3d.tsx (three-terminal cluster) DELETED, replaced by `landing/hero-orbital.tsx` — starfield + 3 slow counter-rotating orbit rings carrying 9 upright agent icon nodes (hover labels) around a pulsing accent core, pointer-tilt parallax via translateZ layers; new orbit-spin/orbit-spin-reverse/core-ping keyframes in the single reduced-motion block. Orchestrator follow-up: removed the now-redundant AgentNetwork letter-circle hero backdrop (component deleted — hero was its only consumer). Build + 18/18 E2E + browser review (desktop/mobile).
- ✅ UI-5 COMPLETE (reference-parity redesign, chunks A→D3): A — 8 new built-in agents (17 total, incl. Data Analyst emitting live vega-lite charts) + evals (114 API tests). B — landing split hero (copy left / 3D tilt cluster right, network backdrop), SectionHeader eyebrow pattern + domain copy sweep (no "roster"/"pillars"), rich Kanban vignette, new Workflows pipeline section. C — real persisted drag-and-drop missions board (PATCH /runs/{id}/tasks/{task_id} allowlist failed→todo re-dispatch / review→todo approve / done→todo re-run, else 409; native HTML5 DnD + optimistic move + poll reconcile). D1-D3 — dashboard kit (components/dash: StatCard/Panel/BarChart/Sparkline) applied to all 10 tool pages: documents, usage (flagship: daily tokens/cost chart + per-agent table), evals, missions (stat row + runs sidebar), agents, templates, automations, playground (experiments stats + workbench panel), voice (status cards + how-it-works), chat (lg+ right-rail conversation stats, streaming untouched). Every chunk: build + 18/18 deterministic E2E + orchestrator browser review. Commits 87e1090…D3. ENV NOTE: a long-running uvicorn holds AUTH_SECRET from process start — after editing .env, restart the API or every web-minted token 401s ("Missing or invalid Authorization header"); also run `next build` and `next dev` from separate cache states (build corrupts the running dev server's .next — restart dev after building).
